[project]
name = "itrs-edge-python-host"
version = "0.1.0"
description = "Python Workers MCP host for the pure-Wasm iTRS Edge service"
requires-python = ">=3.14"
dependencies = [
    "mcp==2.2.0"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
