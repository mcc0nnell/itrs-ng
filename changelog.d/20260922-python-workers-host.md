# Cloudflare Python Workers host seam

Adds cross-language Worker RPC around the existing pure-Wasm edge resolver and a Python Workers MCP host that delegates deterministic resolution without duplicating kernel policy.
