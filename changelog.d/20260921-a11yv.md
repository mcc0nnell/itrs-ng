# A11YV accessibility impact vector

Adds an experimental machine-readable accessibility impact/remediation vector, deterministic C/Wasm scorer, JSON evaluation schema, and MCP `evaluate_accessibility` tool. The numeric score is explicitly separate from WCAG conformance.
