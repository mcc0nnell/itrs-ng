# iTRS Edge deterministic effect kernel

Adds the first iTRS Edge vertical slice: canonical MessagePack envelopes, a stateless effect-driven execution kernel, deterministic resource-snapshot resume, fail-closed faults, and byte-stable replay tests over the existing Number resolver.
